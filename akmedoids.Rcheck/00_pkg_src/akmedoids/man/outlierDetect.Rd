% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/outlierDetect.R
\name{outlierDetect}
\alias{outlierDetect}
\title{Outlier detection and replacements}
\usage{
outlierDetect(traj, id_field = FALSE, method = 1, threshold = 0.95, hortz_tolerance = 1, replace_with = 1)
}
\arguments{
\item{traj}{A matrix or dataframe with each row representing the trajectory of observations of a unique location. The columns show the observations at consecutive time steps.}

\item{id_field}{Whether the first column is a unique (id) field. Default: \code{FALSE}}

\item{method}{Specify the method for identifying the outlier. Available methods: \code{1}: \code{quantile} method, and \code{2}: \code{manual} method".}

\item{threshold}{The cut off value for an observation to be flagged as outlier. For \code{quantile} method, \code{threshold} options are: a numeric vector of probability between \code{[0,1]}. For \code{manual} method, \code{threshold} options are: \code{a user-specified value}}

\item{hortz_tolerance}{[Default: \code{1}]. Specifying the count of outlier observations that must exist in a trajectory in order for the trajectory to be considered an \code{outlier}.}

\item{replace_with}{How to replace the outlier observations. [Default: \code{1}] - mean value of all the observations in the column in which an outlier observation is located. Other options are: \code{2}: Mean value of row in which the observation is found, \code{3}: To remove the outlier trajectory.}
}
\value{
A dataframe with outlier observations replaced or trajectories containing outlier observation removed.
}
\description{
This function identifies outlier observations in the trajectories and determine whether to replace them or remove the trajectories accordingly.
}
\details{
Given a matrix or data.frame with some suspected outlier observations, this function identified those observations based on the method chosen and replace all the observations accordingly.
}
\examples{
traj <- gm.crime.sample1
traj <- dataImputation(traj, id_field=TRUE, replace_with=1)
outlierDetect(traj, id_field = FALSE, method = 1, threshold = 0.95, hortz_tolerance = 1, replace_with = 1)
}
